# Phase 22: Fix README default seed discrepancy
**Priority:** P3 — Trivial doc alignment  
**File:** `README.md`

## Goal
Align README documentation with actual CLI default behavior for Monte Carlo seed.

## Tasks
1. Check actual CLI default in `main.rs` / `pipeline.rs`:
   - If CLI default is `None` and `optimization.rs` treats `None` as seed `1` (via `if seed == 0 { 1 } else { seed }`), update README to state default is `1` (not `1337`).
   - Alternatively, change CLI default to `Some(1337)` and document that. Pick one and apply consistently.
2. Update the `--seed` description in README to match the chosen default.
3. Update `alpha_readiness_2026-07-05.md` Checklist #11 if referenced.

## Validation
- `cdg collect --help` / `cdg backtest --help` shows the documented default.
- README paragraph matches.

## Why Included
Misleading defaults erode trust. Alpha users reproduce runs using the documented seed and get different results. One-line doc fix.
