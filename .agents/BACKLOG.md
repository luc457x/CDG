# Backlog (BACKLOG.md)

Unvalidated ideas. Out of scope.

## Use

- Add idea/feature with short description.
- If approved, spec in [SPEC.md](./SPEC.md) + tasks in [TASKS.md](./TASKS.md).
- Never implement directly from backlog.

## Items

- **Context Utility Tools** — Implement functions to query context (e.g. listing supported coins, trending coins, pinging coingecko/yahoo servers, and fetching raw OHLCV for external visualization tools).
- **JSON Output Mode** — Implement an option/flag to format all outputs (pipeline results, logs, utility query responses) strictly as JSON for easy parsing by external tools and scripts.
