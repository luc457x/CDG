# RTK Installation

If `rtk` missing entirely, run appropriate automated installer:

- **Windows 11 (PowerShell)**: Execute [install.ps1](../scripts/install.ps1) to handle dependency checks and install `rtk` automatically.
- **Linux (Bash)**: Execute [install.sh](../scripts/install.sh) to handle dependency checks and install `rtk` automatically.